using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DotInfo : MonoBehaviour
{
    public GameObject textDotInfo;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // have 1 method to set active for textdotinfo and 1 method tosetinactive for textdotinfo
    // call methods in couritine, when dot is true set text active, when dot false set inactive

    // Update is called once per frame
    void Update()
    {
        
    }
}
